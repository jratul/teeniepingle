interface Props {
  name: string;
  img: string;
  season: number;
  royal?: boolean;
  legend?: boolean;
}

export default function PingItem({ name, img, season, royal, legend }: Props) {
  return (
    <div>
      <div></div>
    </div>
  );
}
